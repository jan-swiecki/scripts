list="$(find . -maxdepth 1 -type f | grep -v 'install\|README')"

for l in $list; do
  sudo cp -f "$(realpath `echo $PWD/$l`)" "/usr/local/bin/$l"
  echo "installed at /usr/local/bin/$(basename "$l")"
done